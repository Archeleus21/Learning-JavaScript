//objects:
/* var myArray = [1,2,3,4,5];
   var myRE= /hello/;

//the date object:
  var today = new Date();
  var y2k = new Date(2000,0,1);
  vat y2k = new Date(2000,0,1,0,0,0);

  today.getMonth();  //returns 0-11
  today.getFullYear(); //YYYY not zero based
  today.getDate();
  today.getDay();
  today.getHours();

  today.setMonth(5);
  today.setFullYeare(2012);
  today.setDay(0);

//Math object:
  var x = 200.6;
  var y = Math.round(x);

  var a = 200, b = 10000, c = 4;
  var biggest = Math.max(a,b,c);
  var smallest = Math.max(a,b,c);

  Math.PI, Math.random(), Math.sqrt(), Math.log();

//other objects:
Array, RegExp, Date, Math

//examples of creating objects using built in classes
  var today = new Date();
  var myArray = new Array(1,2,3);
  var myRegExp = new RegExp("hello");

  
//objects are smart, they have methods and properties
*/

//primitives:
/*var myNumber = 123;
  var isValid = true;

//prmitives simply store a value or data
*/
